@go.Package { name = "github.com/zackarysantana/velocity/gen/pkl/prebuilts/git" }
module git_prebuilt

import "package://pkg.pkl-lang.org/pkl-go/pkl.golang@0.5.3#/go.pkl" 

import "../primitives/command.pkl"

function clone() = new command.PrebuiltCommand {
    prebuilt = "git.clone"
}

class CloneExactArgs {
    directory: String?
}

function cloneExact(args: CloneExactArgs) = new command.PrebuiltCommand {
    prebuilt = "git.clone"
    params {
        ["directory"] = args.directory
    }
}